package main

// func main() {
/* 	a := new([]int)
	// b := make([]int, 0)
	fmt.Printf("a: %v\n", *a)
	b := [4]int{1, 2, 3, 4}
	fmt.Printf("len(b): %v\n", len(b))
} */

// make 和 new 都是用来分配内存的。
// 区别在与 make 只用于 slice、map 以及 channel 的初始化，返回的还是这三个引用类型本身；
// new可以用与任意类型并返回它们的零值，返回的是指针。
